import {iOSColors} from 'react-native-typography';
import {
  flex,
  leftRightMargin,
  topBottomMargin,
  leftRightPadding,
  topBottomPadding,
} from './utilities';

const theme = {
  ...iOSColors,
  flex,
  leftRightMargin,
  topBottomMargin,
  leftRightPadding,
  topBottomPadding,
};

export default theme;
