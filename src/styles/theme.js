import {
  flex,
  leftRightMargin,
  topBottomMargin,
  leftRightPadding,
  topBottomPadding,
} from './utilities';

const theme = {
  primary: '#fff',
  secondary: '#f55242',
  dark: 'black',
  grey: 'rgba(0,0,0,.54)',
  red: '#ec4c4c',
  green: '#00e68a',

  flex,
  leftRightMargin,
  topBottomMargin,
  leftRightPadding,
  topBottomPadding,
};

export default theme;
